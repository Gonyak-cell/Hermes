# Hermes Platform Operations Stability Phase Ledger (P341-P500)

Status date: 2026-05-31

This ledger starts after the Hermes v1.0 freeze and the Trading Pack P340 verified
baseline. Its purpose is not to add risky runtime behavior. It turns the harness
into a more reproducible, observable, recoverable, and operator-safe platform.

## Current Boundary

- Platform v1.0 source baseline: Phase 312.
- Trading pack completion baseline: Phase 340.
- Verified P340 transfer bundle SHA256: `1a1563a47e2f6704e0f25be56a4c74069863e97c6312e0b0348088ccd231051d`.
- Verified P340 history baseline commit: `0abc97b`.
- Mac replay stabilization commit: `8e4323d`.
- Hermes Desktop posture: optional read-only operator companion, not source of truth.
- Trading posture: research, backtest, and paper are active; shadow-live, limited-live, full-auto, live order submission, broker writes, and automatic order submission stay disabled by default.

## Phase Ranges

| Range | Scope | Current status | Stability note |
| --- | --- | --- | --- |
| P341-P360 | Reproducibility baseline | complete | Pin Node/npm policy, lockfile policy, P340 provenance, deterministic runtime baseline artifact, and `platform:runtime-baseline`. |
| P361-P380 | Unified platform and trading checks | complete | Add `trading:release-check`, `platform:ops-check`, `platform:release-check`, and receipt-readiness closeout so operators do not rely on remembered command order. |
| P381-P400 | Trading safety regression | complete | Fail immediately if live, full-auto, automatic order submission, broker credential, generic order, or mutating trading routes become enabled. |
| P401-P420 | Promotion receipt gates | active | Require independent human approval receipts for research -> backtest -> paper -> shadow -> limited-live -> full-auto promotion claims. |
| P421-P440 | Execution and secret isolation | planned | Keep simulated and live broker adapters schema/API separated; live credentials are external secret handles only. |
| P441-P460 | Recovery drills | planned | Cover stale data, exchange outage, failed fill, partial fill, duplicate order intent, kill switch, and rollback-to-paper as dry-run receipt drafts. |
| P461-P480 | Operator observability | planned | Show why live/future mutation is blocked: risk halt, missing approval, disabled route, rollback target, and Desktop read-only boundary. |
| P481-P500 | Platform operations freeze | planned | Freeze the operations layer with unified checks, reproducibility evidence, recovery proof, and safety boundary invariants. |

## P341-P360 Acceptance Criteria

- P341: `platform:runtime-baseline` validates `.nvmrc`, `.node-version`, `.npmrc`, `packageManager`, `engines`, `package-lock.json`, this ledger, and the P340 bundle hash without installing dependencies, creating releases, or enabling trading mutation.
- P342: `platform:drift-check` compares the current local runtime/dependency/provenance state to the P341 baseline in memory and validates source fingerprints without overwriting artifacts in `--check`.
- P343: `platform:replay-window` consumes the P342 drift check in memory and maps current-checkout, contract/release, validation/test, artifact-regeneration, cross-OS history, and Trading safety replay windows without executing commands.
- P344: `platform:operator-handoff` turns P343 replay windows into human-reviewable handoff packets with evidence rows, decision rows, owner roles, and next operator actions without executing commands.
- P345: `platform:artifact-guard` closes the runtime/dependency drift-report sequence by proving P341-P345 generated outputs stay under ignored `artifacts/`, platform commands are registered in validation, and `--check` remains no-overwrite.
- P346: `platform:provenance-ledger` records the P340 bundle hash, P340 history baseline commit, Mac replay stabilization commit, release-bundle hash policy, and future signed-tag requirements without creating tags or releases.
- P347: `platform:release-bundle-provenance` expands release-bundle hash and manifest requirements without creating release bundles, tags, signed tags, or releases.
- P348: `platform:signed-tag-provenance` expands future signed-tag policy and gates without running git, materializing signing keys, creating tags, creating signed tags, or publishing releases.
- P349: `platform:provenance-freeze-preflight` maps P341-P349 provenance freeze sources and gates before final freeze without executing checks, creating tags, creating signed tags, creating release bundles, or publishing releases.
- P350: `platform:provenance-freeze` provides final provenance freeze closeout for P346-P350 rows and gates without creating tags, signed tags, release bundles, releases, or protected actions.
- P351: `platform:mac-windows-replay-notes` documents Mac/Windows replay notes for operator handoff without importing history, changing checkout state, regenerating artifacts, or mutating lockfiles.
- P352: `platform:lockfile-policy` records package manager, npmrc, and package-lock policy without dependency installs, package mutation, lockfile mutation, or artifact regeneration.
- P353: `platform:replay-handoff-map` maps replay scopes to owner roles, expected evidence, and next operator actions without executing commands or protected actions.
- P354: `platform:replay-evidence-checklist` records replay evidence expectations without collecting evidence, executing commands, regenerating artifacts, or performing protected actions.
- P355: `platform:replay-handoff-closeout` closes P351-P355 replay handoff readiness without executing replay actions, regenerating artifacts, collecting evidence, or performing protected actions.
- P356: `platform:reproducibility-check-registry` registers P341-P356 reproducibility checks in package scripts and validation, while bridging to future release-check commands without executing checks or requiring future scripts now.
- P357: `platform:reproducibility-evidence-matrix` records grouped reproducibility evidence expectations without collecting evidence, executing checks, or executing future release-check commands.
- P358: `platform:reproducibility-proof-index` maps reproducibility evidence rows to expected proof references without materializing proof, reading artifacts, collecting evidence, or executing checks.
- P359: `platform:reproducibility-operator-review` turns proof rows into human-reviewable operator review rows without completing review, applying approvals, reading artifacts, or executing protected actions.
- P360: `platform:reproducibility-closeout` closes P341-P360 reproducibility readiness and records P361-P380 as the next unified platform and trading check block without executing checks, applying approvals, or protected actions.

## P361-P380 Acceptance Criteria

- P361: `trading:release-check` executes the contract validation, release freeze, and complete Trading Pack check stack in `--check` mode while keeping live trading, full-auto, order submission, broker writes, exchange writes, release publication, git operations, and protected actions disabled.
- P362: `platform:ops-check` verifies runtime baseline, contract validation, control-plane loop runner readiness, dashboard/API smoke readiness, and domain-pack registry health without rebuilding dashboard artifacts, running package commands, or mutating release/trading state.
- P363: `platform:release-check` composes `platform:ops-check -- --check`, `trading:release-check -- --check`, `npm run validate`, `npm test`, `contracts:validate -- --check`, and `release:freeze -- --check` without registering itself in `npm run validate` or executing release/trading mutations.
- P364: `platform:release-check-no-write-audit` verifies the P361-P363 release-check bridge commands have `--check` write guards and no-overwrite tests without executing those commands or mutating artifacts.
- P365: `platform:release-check-evidence-index` maps P361-P364 release-check commands to docs, expected report paths, validation-chain policy, and human-review evidence without executing commands or mutating artifacts.
- P366: `platform:release-check-review-packet` turns P365 evidence rows into human-reviewable release-check review packets with reviewer roles and expected decisions without completing review, applying approvals, executing commands, reading/writing artifacts, or mutating release/trading state.
- P367: `platform:release-check-signoff-ledger` records required human signoff receipts for P366 review rows without completing signoff, applying approvals, materializing receipts, executing commands, reading/writing artifacts, or mutating release/trading state.
- P368: `platform:release-check-signoff-receipt-template` creates human-fillable signoff receipt templates from P367 rows without completing receipts, applying approvals, materializing receipts, executing commands, reading/writing artifacts, or mutating release/trading state.
- P369: `platform:release-check-signoff-receipt-intake` queues P368 receipt templates for external human input without receiving receipts, validating receipts, completing signoff, applying approvals, executing commands, reading/writing artifacts, or mutating release/trading state.
- P370: `platform:release-check-signoff-closeout` closes the P365-P370 signoff-readiness subchain as ready for external human receipt collection without receiving receipts, validating receipts, completing signoff, applying approvals, executing commands, reading/writing artifacts, or mutating release/trading state.
- P371: `platform:release-check-status-ledger` summarizes the P361-P370 release-check command, evidence, review, signoff, receipt, and closeout status in a read-only operator ledger while keeping human receipts pending and without receiving receipts, validating receipts, applying approvals, executing commands, reading/writing artifacts, or mutating release/trading state.
- P372: `platform:release-check-receipt-queue` turns the P371 status ledger into a deterministic external human receipt collection queue without receiving receipts, validating receipts, completing signoff, applying approvals, executing commands, reading/writing artifacts, or mutating release/trading state.
- P373: `platform:release-check-receipt-validation-rules` declares deterministic future validation rules for queued release-check receipts without receiving receipt payloads, validating receipts, completing signoff, applying approvals, executing commands, reading/writing artifacts, or mutating release/trading state.
- P374: `platform:release-check-receipt-workspace` declares reviewer receipt workspace rows and editable receipt fields without materializing receipt input files, receiving payloads, validating receipts, completing signoff, applying approvals, executing commands, reading/writing artifacts, or mutating release/trading state.
- P375: `platform:release-check-receipt-workspace-merge` declares future merge rows for release-check receipt workspaces without reading actor workspace files, materializing merged receipt input, receiving payloads, validating receipts, completing signoff, applying approvals, executing commands, reading/writing artifacts, or mutating release/trading state.
- P376: `platform:release-check-receipt-merge-preflight` declares future merge-validation preflight rows for release-check receipts without reading actor workspace files, materializing merged receipt input, receiving payloads, validating receipts, completing signoff, applying approvals, executing commands, reading/writing artifacts, or mutating release/trading state.
- P377: `platform:release-check-receipt-validation-packet` declares future validation packet rows for release-check receipts without reading actor workspace files, materializing merged receipt input, receiving payloads, validating receipts, completing signoff, applying approvals, executing commands, reading/writing artifacts, or mutating release/trading state.
- P378: `platform:release-check-receipt-approval-plan` declares future approval-plan rows for release-check receipts without reading actor workspace files, materializing merged receipt input, receiving payloads, validating receipts, completing signoff, applying approvals, executing commands, reading/writing artifacts, or mutating release/trading state.
- P379: `platform:release-check-receipt-approval-closeout` closes the future approval-plan readiness layer for release-check receipts without reading actor workspace files, materializing merged receipt input, receiving payloads, validating receipts, completing signoff, applying approvals, executing commands, reading/writing artifacts, or mutating release/trading state.
- P380: `platform:release-check-receipt-closeout` closes the P361-P380 release-check receipt readiness chain and records P381 as the next trading safety regression block without reading actor workspace files, receiving payloads, validating receipts, completing signoff, applying approvals, executing commands, reading/writing artifacts, or mutating release/trading state.
- `trading:release-check` runs the complete Trading Pack validation stack, contract validation, release freeze, and trading no-write checks in one command.
- `platform:ops-check` verifies runtime baseline, contracts, control-plane loop, dashboard/API smoke readiness, and domain-pack registry health.
- `platform:release-check` composes `platform:ops-check`, `trading:release-check`, `npm run validate`, `npm test`, `contracts:validate -- --check`, and `release:freeze -- --check`.
- Every command supports `--check` without overwriting existing artifacts on failure.

## P381-P400 Acceptance Criteria

- P381: `trading:safety-regression-fixtures` declares fail-fast regression fixtures that block when `limited_live_enabled`, `full_auto_enabled`, `automatic_order_submission_allowed`, or `live_order_submission_allowed` is true without enabling live trading, full-auto, order submission, broker writes, exchange writes, command execution, artifact mutation, or protected actions.
- P382: `trading:route-inventory-fixtures` declares active-route inventory fixtures that block when mutating Trading routes, broker credential routes, live broker write routes, or generic order submission routes appear outside disabled route evidence without enabling live trading, order submission, broker writes, exchange writes, command execution, artifact mutation, or protected actions.
- P383: `trading:approval-absence-fixtures` declares fail-fast approval absence fixtures that prove missing limited-live, full-auto, first-trade, promotion, manual-resume, and risk-override approvals keep live trading, full-auto, order submission, live adapters, credential lookup, broker writes, and exchange writes blocked without applying approvals, accepting receipts, executing commands, mutating artifacts, or protected actions.
- P384: `trading:live-adapter-disabled-fixtures` declares fail-fast live adapter fixtures that prove execution live adapters, broker/exchange interfaces, shadow live data adapters, limited-live adapter references, and full-auto failover policies keep live adapter activation, external network access, live writes, broker writes, exchange writes, and order submission blocked without executing commands, mutating artifacts, or protected actions.
- P385: `trading:credential-lookup-disabled-fixtures` declares fail-fast credential lookup fixtures that prove credential broker lookup, plaintext secrets, model-context secrets, vendor credentials, external API keys, paper/shadow credentials, limited-live credentials, full-auto external credentials, and secret logging remain disabled without reading secret values, materializing credentials, executing commands, mutating artifacts, or protected actions.
- P386: `trading:broker-write-disabled-fixtures` declares fail-fast broker write fixtures that prove broker adapter write methods, live submit state, order submission, live cancel routes, real order side effects, limited-live approval gates, and full-auto broker failover writes remain disabled without submitting orders, cancelling live orders, executing commands, mutating artifacts, or protected actions.
- P387: `trading:exchange-write-disabled-fixtures` declares fail-fast exchange write fixtures that prove crypto exchange adapter write methods, live submit state, order submission, live cancel routes, real order side effects, limited-live approval gates, and full-auto broker/exchange failover writes remain disabled without submitting orders, cancelling live orders, executing commands, mutating artifacts, or protected actions.
- P388: `trading:safety-boundary-fixtures` consolidates P381-P387 safety fixture layers and fails when any layer is missing, unsafe, or capable of enabling limited-live, full-auto, order submission, live adapters, credential lookup, broker writes, exchange writes, artifact mutation, command execution, or protected actions.
- P389: `trading:manual-resume-disabled-fixtures` declares fail-fast halt, rollback, disaster recovery, and manual resume fixtures that prove resume routes, protected action routes, live order cancellation, external broker recovery, and non-paper recovery remain disabled without executing halt, resume, rollback, recovery, broker/exchange, artifact mutation, command execution, or protected actions.
- P390: `trading:risk-override-disabled-fixtures` declares fail-fast risk override fixtures that prove override paths require human approval, protected action gates, and future receipts while risk override without human review, override routes, advice/recommendation generation, order intent generation, live trade, live execution, artifact mutation, command execution, and protected actions remain disabled.
- P391: `trading:promotion-disabled-fixtures` declares fail-fast promotion fixtures that prove model, backtest, paper-to-shadow, shadow-to-limited-live, limited-live-to-full-auto, and full-auto approval paths remain human-gated, receipt-blocked, route-disabled, and unable to enable shadow-live, limited-live, full-auto, order intent, live execution, artifact mutation, command execution, or protected actions.
- P392: `trading:first-trade-disabled-fixtures` declares fail-fast first-trade fixtures that prove limited-live first-trade submission remains blocked without approval receipts, first-trade confirmation, order capacity, enabled live order routes, live cancel, live fills, real order reporting, artifact mutation, command execution, or protected actions.
- P393: `trading:order-intent-disabled-fixtures` declares fail-fast order-intent fixtures that prove signal, model, backtest, risk, shadow order intent, and execution submit paths remain disabled, non-executable, route-disabled, throttled, and unable to submit real orders, live execution, artifact mutation, command execution, or protected actions.
- P394: `trading:market-order-disabled-fixtures` declares fail-fast market-order fixtures that prove research policy, strategy constraints, shadow intents, execution order-type whitelist, limited-live order routes, and full-auto order routes keep market orders disabled without submitting orders, enabling live execution, mutating artifacts, executing commands, or protected actions.
- P395: `trading:leverage-disabled-fixtures` declares fail-fast leverage and margin fixtures that prove research policy, asset capability flags, strategy constraints, backtest position sizing, and risk leverage/margin gates keep leverage and margin disabled without generating order intents, enabling live execution, mutating artifacts, executing commands, or protected actions.
- P396: `trading:short-selling-disabled-fixtures` declares fail-fast short-selling fixtures that prove research policy, asset capability flags, strategy constraints, paper/order-intent examples, and risk short-selling capability gates keep short selling disabled without generating order intents, enabling live execution, broker writes, mutating artifacts, executing commands, or protected actions.
- P397: `trading:order-frequency-throttle-fixtures` declares fail-fast order-frequency throttle fixtures that prove signal, risk, execution, paper/shadow, limited-live, and full-auto surfaces keep order generation and submission throttled to zero without generating order intents, enabling live execution, broker writes, exchange writes, mutating artifacts, executing commands, or protected actions.
- P398: `trading:loss-streak-cooldown-fixtures` declares fail-fast loss-streak cooldown fixtures that prove risk cooldown, halt policy, paper loss review, limited-live halt gates, execution manual resume, and full-auto disable policies remain human-gated and non-mutating without enabling order intent, live execution, broker writes, exchange writes, artifact mutation, command execution, or protected actions.
- P399: `trading:model-degradation-halt-fixtures` declares fail-fast model degradation halt fixtures that prove risk, model-improvement, research model, limited-live, and full-auto degradation paths remain source-bound, human-gated, promotion-blocked, and non-mutating without deploying models, generating order intents, enabling live execution, broker writes, exchange writes, artifact mutation, command execution, or protected actions.
- P400: `trading:data-outage-halt-fixtures` declares fail-fast data-outage halt fixtures that prove risk, market-data quality, paper/shadow, limited-live, and full-auto failover paths remain armed, read-only, and non-mutating without enabling live feeds, generating order intents, enabling live execution, broker writes, exchange writes, artifact mutation, command execution, or protected actions.
- Regression fixtures fail when `limited_live_enabled`, `full_auto_enabled`, `automatic_order_submission_allowed`, or `live_order_submission_allowed` is true.
- Route inventory fixtures fail when mutating trading routes, broker credential routes, live broker write routes, or generic order submission routes are present.
- Safety checks cover disabled routes, approval absence, live adapter disabled state, credential lookup disabled state, no broker writes, and no exchange writes.

## P401-P420 Acceptance Criteria

- P401: `trading:promotion-receipt-contract-fixtures` declares independent human approval receipt contracts and summary rows for research-to-backtest, backtest-to-paper, paper-to-shadow, shadow-to-limited-live, limited-live-to-full-auto, and full-auto activation claims while keeping governance reports read-only/complete only when real enablement, receipt intake, receipt application, live execution, broker writes, exchange writes, artifact mutation, command execution, and protected actions remain false.
- P402: `trading:promotion-completion-gate-fixtures` turns the P401 receipt contracts into per-stage completion gates proving no higher-stage promotion can be marked complete for enablement without its independent human receipt while receipt intake, receipt validation, receipt application, live execution, order submission, broker writes, exchange writes, artifact mutation, command execution, and protected actions remain false.
- P403: `trading:promotion-governance-readonly-fixtures` proves active or complete promotion governance reports remain read-only evidence and cannot imply real promotion enablement while receipt intake, approval application, shadow-live, limited-live, full-auto, live execution, order submission, broker writes, exchange writes, artifact mutation, command execution, and protected actions remain false.
- P404: `trading:promotion-receipt-intake-queue-fixtures` projects one pending human receipt intake row for each promotion stage and proves receipt payloads are not read, materialized, validated, applied, or converted into promotion enablement while governance remains read-only and shadow-live, limited-live, full-auto, live execution, order submission, broker writes, exchange writes, artifact mutation, command execution, and protected actions remain false.
- P405: `trading:promotion-receipt-validation-rules-fixtures` declares deterministic future validation fields and allowed decisions for queued promotion receipts while receiving no receipt payloads, validating no receipts, applying no approvals, and keeping shadow-live, limited-live, full-auto, live execution, order submission, broker writes, exchange writes, artifact mutation, command execution, and protected actions false.
- P406: `trading:promotion-receipt-workspace-fixtures` exposes human-editable receipt workspace rows for each promotion stage while materializing no receipt input files, receiving no payloads, validating no receipts, applying no approvals, and keeping shadow-live, limited-live, full-auto, live execution, order submission, broker writes, exchange writes, artifact mutation, command execution, and protected actions false.
- P407: `trading:promotion-receipt-workspace-merge-fixtures` declares future workspace merge readiness for each promotion stage while materializing no merged receipt input, receiving no payloads, merging no workspace files, validating no receipts, applying no approvals, and keeping shadow-live, limited-live, full-auto, live execution, order submission, broker writes, exchange writes, artifact mutation, command execution, and protected actions false.
- P408: `trading:promotion-receipt-merge-preflight-fixtures` declares future receipt merge-validation preflight rows for each promotion stage while reading no actor workspace input, materializing no receipt input, receiving no payloads, validating no receipts, applying no approvals, and keeping shadow-live, limited-live, full-auto, live execution, order submission, broker writes, exchange writes, artifact mutation, command execution, and protected actions false.
- P409: `trading:promotion-receipt-validation-packet-fixtures` declares future receipt validation packet rows for each promotion stage while reading no actor workspace input, materializing no receipt input, receiving no payloads, validating no receipts, applying no approvals, and keeping shadow-live, limited-live, full-auto, live execution, order submission, broker writes, exchange writes, artifact mutation, command execution, and protected actions false.
- P410: `trading:promotion-receipt-approval-plan-fixtures` declares future receipt approval plan rows for each promotion stage while reading no actor workspace input, materializing no receipt input, receiving no payloads, validating no receipts, applying no approvals, and keeping shadow-live, limited-live, full-auto, live execution, order submission, broker writes, exchange writes, artifact mutation, command execution, and protected actions false.
- P411: `trading:promotion-receipt-approval-closeout-fixtures` declares future receipt approval closeout rows for each promotion stage while reading no actor workspace input, materializing no receipt input, receiving no payloads, validating no receipts, applying no approvals, and keeping shadow-live, limited-live, full-auto, live execution, order submission, broker writes, exchange writes, artifact mutation, command execution, and protected actions false.
- P412: `trading:promotion-receipt-closeout-fixtures` closes the P401-P412 promotion receipt chain while human receipts remain pending and no actor workspace input, receipt payload, validation, approval application, promotion enablement, live execution, order submission, broker write, exchange write, artifact mutation, command execution, or protected action occurs.
- P413: `trading:promotion-receipt-chain-regression-fixtures` verifies P401-P412 promotion receipt-chain script, validation-chain, and ledger wiring while human receipts remain pending and no receipt payload, validation, approval application, promotion enablement, live execution, order submission, broker write, exchange write, artifact mutation, command execution, or protected action occurs.
- P414: `trading:promotion-receipt-chain-review-fixtures` converts the P413 promotion receipt-chain regression rows into pending human-review packets while no review is completed and no receipt payload, validation, approval application, promotion enablement, live execution, order submission, broker write, exchange write, artifact mutation, command execution, or protected action occurs.
- P415: `trading:promotion-receipt-chain-signoff-ledger-fixtures` records required signoff ledger rows for the P414 promotion receipt-chain review packets while no signoff is completed and no receipt payload, validation, approval application, promotion enablement, live execution, order submission, broker write, exchange write, artifact mutation, command execution, or protected action occurs.
- P416: `trading:promotion-receipt-chain-signoff-template-fixtures` declares human-fillable signoff templates for the P415 promotion receipt-chain signoff ledger rows while no template is materialized, no signoff is completed, and no receipt payload, validation, approval application, promotion enablement, live execution, order submission, broker write, exchange write, artifact mutation, command execution, or protected action occurs.
- P417: `trading:promotion-receipt-chain-signoff-intake-fixtures` queues the P416 promotion receipt-chain signoff templates for external human input while no signoff receipt is received, no validation or approval application occurs, and no promotion enablement, live execution, order submission, broker write, exchange write, artifact mutation, command execution, or protected action occurs.
- P418: `trading:promotion-receipt-chain-signoff-validation-rules-fixtures` declares future validation rules for P417 promotion receipt-chain signoff intake rows while no signoff receipt is received or validated, no approval application occurs, and no promotion enablement, live execution, order submission, broker write, exchange write, artifact mutation, command execution, or protected action occurs.
- P419: `trading:promotion-receipt-chain-signoff-approval-closeout-fixtures` declares future approval-closeout readiness for P418 promotion receipt-chain signoff validation-rule rows while no signoff receipt is received or validated, no approval application occurs, and no promotion enablement, live execution, order submission, broker write, exchange write, artifact mutation, command execution, or protected action occurs.
- P420: `trading:promotion-receipt-chain-signoff-closeout-fixtures` closes the P415-P420 promotion receipt-chain signoff readiness subchain while human receipts remain pending and no signoff receipt is received or validated, no approval application occurs, and no promotion enablement, live execution, order submission, broker write, exchange write, artifact mutation, command execution, or protected action occurs.
- Each promotion stage has an independent approval receipt contract and summary row.
- A higher stage cannot be marked complete for enablement without its required receipt.
- Governance reports may be complete while real enablement remains false.
- Full-auto governance remains complete only as a blocked/default-disabled control-plane state.

## P421-P440 Acceptance Criteria

- P421: `trading:broker-adapter-separation-fixtures` records the simulated/sandbox broker adapter contracts separately from the disabled live adapter contract while default control-plane paths do not import live adapter files and credential lookup, plaintext secrets, provider key material, environment dumps, live execution, broker writes, exchange writes, artifact mutation, command execution, and protected actions remain false.
- P422: `trading:live-adapter-import-boundary-fixtures` records a future live-adapter import boundary across default package scripts, validation chain, trading pack manifest, capability entrypoints, and governance paths while any future live adapter file remains reference-only, non-readable, non-imported, non-executable, and unable to expose credentials, live execution, broker writes, exchange writes, artifact mutation, command execution, or protected actions.
- P423: `trading:secret-handle-boundary-fixtures` records Trading credential contracts, secret logging, policy, Hermes example config, Desktop companion, and secrets-scan gate fixtures as external-handle/reference-only with no plaintext secrets, provider keys, environment dumps, Desktop secret exposure, live execution, broker writes, exchange writes, artifact mutation, command execution, or protected actions.
- P424: `trading:secret-leakage-regression-fixtures` records synthetic secret-leakage regression cases proving external secret-handle placeholders remain allowed while raw provider-key, bearer-token, private-key, environment-dump, and Desktop provider-key visibility patterns remain blocked without materializing secret values or performing credential lookup, trading writes, artifact mutation, command execution, release publication, git operations, or protected actions.
- P425: `trading:secret-scan-gate-fixtures` records the bridge from P424 secret-leakage regressions into the existing platform Secrets Scan Gate package command, control-plane loop step, contract-validation requirement, Review API surfaces, and documentation boundary while no real secret values, `.env` files, Desktop config content, credential lookup, trading writes, artifact mutation, release publication, git operations, or protected actions are read or executed.
- P426: `trading:secret-scan-attention-fixtures` records the operator attention surface for Secrets Scan Gate leakage through Review Dashboard stage/counters/fix actions, Review API filters, Review API docs, and attention-state documentation while no secret values, `.env` files, Desktop config content, provider keys, credential lookup, trading writes, artifact mutation, release publication, git operations, or protected actions are read or executed.
- P427: `trading:secret-scan-fail-closed-fixtures` records that P300 Secrets Scan Gate rows fail closed for credential, token, env, Desktop config, provider-key, protected config write, and secret-transfer leakage signals while the P427 probe stays synthetic/redacted and no secret values, `.env` files, Desktop config content, credential lookup, trading writes, artifact mutation, release publication, git operations, or protected actions are read or executed.
- P428: `trading:secret-scan-remediation-fixtures` records that Secrets Scan Gate remediation actions remain advisory and human-review-only by proving dashboard fix actions are non-protected, the control-plane action plan does not auto-map secret fix/rerun actions to commands, pending human receipts do not trigger protected actions, and no automatic secret redaction, deletion, rotation, apply, credential lookup, trading write, artifact mutation, release publication, git operation, or protected action occurs.
- P429: `trading:secret-scan-remediation-receipt-template-fixtures` records pending human receipt templates for Secrets Scan Gate remediation review, external removal/rotation references, clean rerun evidence, and closeout decisions while no receipt payload is received, validated, applied, or converted into approval and no secret value, `.env` content, Desktop config content, credential lookup, trading write, artifact mutation, release publication, git operation, or protected action occurs.
- P430: `trading:secret-scan-remediation-receipt-intake-fixtures` queues one pending human input row for each P429 Secrets Scan Gate remediation receipt template while no receipt payload is received, validated, applied, or converted into approval and no secret value, `.env` content, Desktop config content, credential lookup, trading write, artifact mutation, release publication, git operation, or protected action occurs.
- P431: `trading:secret-scan-remediation-receipt-validation-rules-fixtures` declares deterministic future validation rules for each P430 Secrets Scan Gate remediation receipt intake row while no receipt payload is received, validated, applied, or converted into approval and no secret value, `.env` content, Desktop config content, credential lookup, trading write, artifact mutation, release publication, git operation, or protected action occurs.
- P432: `trading:secret-scan-remediation-receipt-workspace-fixtures` declares external human workspace rows and editable receipt fields for each P431 Secrets Scan Gate remediation receipt validation-rule row while no receipt input file is materialized, no actor workspace payload is read, no receipt payload is received or validated, no approval is applied, and no secret value, `.env` content, Desktop config content, credential lookup, trading write, artifact mutation, release publication, git operation, or protected action occurs.
- P433: `trading:secret-scan-remediation-receipt-workspace-merge-fixtures` declares future merge rows for each P432 Secrets Scan Gate remediation receipt workspace row while no actor workspace file or payload is read, no receipt input or merged receipt input is materialized, no receipt payload is received or validated, no approval is applied, and no secret value, `.env` content, Desktop config content, credential lookup, trading write, artifact mutation, release publication, git operation, or protected action occurs.
- P434: `trading:secret-scan-remediation-receipt-merge-preflight-fixtures` declares future merge validation preflight rows for each P433 Secrets Scan Gate remediation receipt workspace-merge row while no actor workspace file or payload is read, no receipt input or merged receipt input is materialized, no receipt payload is received or validated, no approval is applied, and no secret value, `.env` content, Desktop config content, credential lookup, trading write, artifact mutation, release publication, git operation, or protected action occurs.
- P435: `trading:secret-scan-remediation-receipt-validation-packet-fixtures` declares future validation packet rows for each P434 Secrets Scan Gate remediation receipt merge-preflight row while no actor workspace file or payload is read, no receipt input or merged receipt input is materialized, no receipt payload is received or validated, no approval is applied, and no secret value, `.env` content, Desktop config content, credential lookup, trading write, artifact mutation, release publication, git operation, or protected action occurs.
- P436: `trading:secret-scan-remediation-receipt-approval-plan-fixtures` declares future approval plan rows for each P435 Secrets Scan Gate remediation receipt validation-packet row while no actor workspace file or payload is read, no receipt input or merged receipt input is materialized, no receipt payload is received or validated, approval application is not made ready, no approval is applied, and no secret value, `.env` content, Desktop config content, credential lookup, trading write, artifact mutation, release publication, git operation, or protected action occurs.
- P437: `trading:secret-scan-remediation-receipt-approval-closeout-fixtures` declares future approval closeout rows for each P436 Secrets Scan Gate remediation receipt approval-plan row while no actor workspace file or payload is read, no receipt input or merged receipt input is materialized, no receipt payload is received or validated, approval application is not made ready, no approval is applied, and no secret value, `.env` content, Desktop config content, credential lookup, trading write, artifact mutation, release publication, git operation, or protected action occurs.
- P438: `trading:secret-scan-remediation-receipt-closeout-fixtures` declares future receipt chain closeout rows for each P437 Secrets Scan Gate remediation receipt approval-closeout row while human receipts remain pending, no actor workspace file or payload is read, no receipt input or merged receipt input is materialized, no receipt payload is received or validated, approval application is not made ready, no approval is applied, and no secret value, `.env` content, Desktop config content, credential lookup, trading write, artifact mutation, release publication, git operation, or protected action occurs.
- P439: `trading:secret-scan-remediation-receipt-chain-regression-fixtures` verifies P429-P438 Secrets Scan Gate remediation receipt chain package, validation, and ledger wiring while human receipts remain pending, no actor workspace file or payload is read, no receipt input or merged receipt input is materialized, no receipt payload is received or validated, approval application is not made ready, no approval is applied, and no secret value, `.env` content, Desktop config content, credential lookup, trading write, artifact mutation, release publication, git operation, or protected action occurs.
- P440: `trading:secret-scan-remediation-receipt-chain-review-fixtures` declares pending human-review packets for each P439 Secrets Scan Gate remediation receipt chain-regression row while no review is completed, no approval is applied, human receipts remain pending, no receipt payload is received or validated, and no secret value, `.env` content, Desktop config content, credential lookup, trading write, artifact mutation, release publication, git operation, or protected action occurs.
- P441: `trading:secret-scan-remediation-receipt-chain-signoff-ledger-fixtures` declares pending signoff ledger rows for each P440 Secrets Scan Gate remediation receipt chain-review row while no signoff is completed, no approval is applied, human receipts remain pending, no receipt payload is received or validated, and no secret value, `.env` content, Desktop config content, credential lookup, trading write, artifact mutation, release publication, git operation, or protected action occurs.
- P442: `trading:secret-scan-remediation-receipt-chain-signoff-template-fixtures` declares human-fillable signoff templates for each P441 Secrets Scan Gate remediation receipt chain signoff-ledger row while no template is materialized, no signoff is completed, no approval is applied, human receipts remain pending, no receipt payload is received or validated, and no secret value, `.env` content, Desktop config content, credential lookup, trading write, artifact mutation, release publication, git operation, or protected action occurs.
- P443: `trading:secret-scan-remediation-receipt-chain-signoff-intake-fixtures` queues the P442 Secrets Scan Gate remediation receipt chain signoff templates for external human input while no signoff receipt is received, no receipt payload is validated, no signoff is completed, no approval is applied, and no secret value, `.env` content, Desktop config content, credential lookup, remediation action, trading write, artifact mutation, release publication, git operation, or protected action occurs.
- P444: `trading:secret-scan-remediation-receipt-chain-signoff-validation-rules-fixtures` declares future validation rules for each P443 Secrets Scan Gate remediation receipt chain signoff-intake row while no signoff receipt is received, no receipt payload is validated, no signoff is completed, no approval is applied, and no secret value, `.env` content, Desktop config content, credential lookup, remediation action, trading write, artifact mutation, release publication, git operation, or protected action occurs.
- P445: `trading:secret-scan-remediation-receipt-chain-signoff-approval-closeout-fixtures` declares future approval-closeout readiness for each P444 Secrets Scan Gate remediation receipt chain signoff validation-rule row while no signoff receipt is received, no receipt payload is validated, no approval closeout is completed, no signoff is completed, no approval is applied, and no secret value, `.env` content, Desktop config content, credential lookup, remediation action, trading write, artifact mutation, release publication, git operation, or protected action occurs.
- P446: `trading:secret-scan-remediation-receipt-chain-signoff-closeout-fixtures` declares signoff closeout readiness for each P445 Secrets Scan Gate remediation receipt chain signoff approval-closeout row while no signoff receipt is received, no receipt payload is validated, no signoff closeout is completed, no signoff is completed, no approval is applied, and no secret value, `.env` content, Desktop config content, credential lookup, remediation action, trading write, artifact mutation, release publication, git operation, or protected action occurs.
- P447: `trading:secret-scan-remediation-receipt-chain-broker-adapter-separation-fixtures` records post-closeout broker adapter separation for the P446 Secrets Scan Gate remediation receipt chain while simulated/sandbox adapter contracts remain separate from disabled live adapter contracts, default control-plane paths do not import live adapter files, and no secret value, `.env` content, Desktop config content, credential lookup, remediation action, live execution, broker write, exchange write, artifact mutation, release publication, git operation, or protected action occurs.
- P448: `trading:secret-scan-remediation-receipt-chain-live-adapter-import-boundary-fixtures` records future live-adapter import boundaries for the P447 Secrets Scan Gate remediation receipt chain while default package, validation, pack manifest, capability entrypoint, and governance paths do not import or enable live adapter files, future live adapter paths remain reference-only and unread, and no secret value, `.env` content, Desktop config content, credential lookup, remediation action, live execution, broker write, exchange write, artifact mutation, release publication, git operation, or protected action occurs.
- P449: `trading:secret-scan-remediation-receipt-chain-secret-handle-boundary-fixtures` records Trading credential contracts, secret logging, policy, Hermes example config, Desktop companion, and secrets-scan gate fixtures for the P448 Secrets Scan Gate remediation receipt chain as external-handle/reference-only with no plaintext secrets, provider keys, environment dumps, Desktop secret exposure, secret value reads, `.env` reads, Desktop config content reads, credential lookup, remediation action, live execution, broker write, exchange write, artifact mutation, release publication, git operation, or protected action.
- P450: `trading:secret-scan-remediation-receipt-chain-secret-leakage-regression-fixtures` records synthetic secret-leakage regression cases for the P449 Secrets Scan Gate remediation receipt chain proving external secret-handle placeholders remain allowed as references while raw provider-key, bearer-token, private-key, environment-dump, and Desktop provider-key visibility patterns remain blocked, and no secret value, `.env` content, Desktop config content, credential lookup, remediation action, live execution, broker write, exchange write, artifact mutation, release publication, git operation, or protected action occurs.
- P451: `trading:secret-scan-remediation-receipt-chain-secret-scan-gate-fixtures` records the P450 Secrets Scan Gate remediation receipt chain bridge into the existing platform `security:secrets-scan-gate` command, control-plane loop, contract validation, Review API surfaces, and documentation boundary while no secret value, `.env` content, Desktop config content, credential lookup, remediation action, live execution, broker write, exchange write, artifact mutation, release publication, git operation, or protected action occurs.
- P452: `trading:secret-scan-remediation-receipt-chain-secret-scan-attention-fixtures` records the P451 Secrets Scan Gate remediation receipt chain operator attention surface through Review Dashboard stage/counters/fix action, Review API filters, Review API docs, and attention-state documentation while no secret value, `.env` content, Desktop config content, credential lookup, remediation action, live execution, broker write, exchange write, artifact mutation, release publication, git operation, or protected action occurs.
- P453: `trading:secret-scan-remediation-receipt-chain-secret-scan-fail-closed-fixtures` records that the P452 Secrets Scan Gate remediation receipt chain attention surface and P300 Secrets Scan Gate rows fail closed for credential, token, env, Desktop config, provider-key, protected config write, and secret-transfer leakage signals while the P453 probe stays synthetic/redacted and no secret value, `.env` content, Desktop config content, Desktop provider key, credential lookup, remediation action, live execution, broker write, exchange write, artifact mutation, release publication, git operation, or protected action occurs.
- P454: `trading:secret-scan-remediation-receipt-chain-secret-scan-remediation-fixtures` records the P453 Secrets Scan Gate remediation receipt chain advisory remediation surface through Review Dashboard fix actions, control-plane action-plan mapping, and human-gate receipt instructions while no automatic secret fix command, redaction, deletion, rotation, receipt application, secret value, `.env` content, Desktop config content, Desktop provider key, credential lookup, remediation action, live execution, broker write, exchange write, artifact mutation, release publication, git operation, or protected action occurs.
- P455: `trading:secret-scan-remediation-receipt-chain-secret-scan-remediation-receipt-template-fixtures` declares pending human receipt templates for the P454 Secrets Scan Gate remediation receipt chain advisory remediation surface while no receipt payload is received, validated, applied, or converted into approval and no secret value, `.env` content, Desktop config content, Desktop provider key, credential lookup, automatic redaction, deletion, rotation, remediation action, trading write, artifact mutation, release publication, git operation, or protected action occurs.
- P456: `trading:secret-scan-remediation-receipt-chain-secret-scan-remediation-receipt-intake-fixtures` queues one pending human input row for each P455 Secrets Scan Gate remediation receipt chain advisory-remediation receipt template while no receipt payload is received, validated, applied, or converted into approval and no secret value, `.env` content, Desktop config content, Desktop provider key, credential lookup, automatic redaction, deletion, rotation, remediation action, trading write, artifact mutation, release publication, git operation, or protected action occurs.
- P457: `trading:secret-scan-remediation-receipt-chain-secret-scan-remediation-receipt-validation-rules-fixtures` declares future validation rules for each P456 Secrets Scan Gate remediation receipt chain advisory-remediation receipt intake row while no receipt payload is received, validated, applied, or converted into approval and no secret value, `.env` content, Desktop config content, Desktop provider key, credential lookup, automatic redaction, deletion, rotation, remediation action, trading write, artifact mutation, release publication, git operation, or protected action occurs.
- P458: `trading:secret-scan-remediation-receipt-chain-secret-scan-remediation-receipt-workspace-fixtures` declares external human workspace rows for each P457 Secrets Scan Gate remediation receipt chain advisory-remediation receipt validation-rule row while no receipt file or payload is materialized, read, validated, applied, or converted into approval and no secret value, `.env` content, Desktop config content, Desktop provider key, credential lookup, automatic redaction, deletion, rotation, remediation action, trading write, artifact mutation, release publication, git operation, or protected action occurs.
- P459: `trading:secret-scan-remediation-receipt-chain-secret-scan-remediation-receipt-workspace-merge-fixtures` declares future merge rows for each P458 Secrets Scan Gate remediation receipt chain advisory-remediation receipt workspace row while no actor workspace file or payload is read, no receipt input or merged receipt input is materialized, no receipt payload is received, validated, applied, or converted into approval and no secret value, `.env` content, Desktop config content, Desktop provider key, credential lookup, automatic redaction, deletion, rotation, remediation action, trading write, artifact mutation, release publication, git operation, or protected action occurs.
- P460: `trading:secret-scan-remediation-receipt-chain-secret-scan-remediation-receipt-merge-preflight-fixtures` declares future merge validation preflight rows for each P459 Secrets Scan Gate remediation receipt chain advisory-remediation receipt workspace-merge row while no actor workspace file or payload is read, no receipt input or merged receipt input is materialized, no receipt payload is received, validated, applied, or converted into approval and no secret value, `.env` content, Desktop config content, Desktop provider key, credential lookup, automatic redaction, deletion, rotation, remediation action, trading write, artifact mutation, release publication, git operation, or protected action occurs.
- P461: `trading:secret-scan-remediation-receipt-chain-secret-scan-remediation-receipt-validation-packet-fixtures` declares future validation packet rows for each P460 Secrets Scan Gate remediation receipt chain advisory-remediation receipt merge-preflight row while no actor workspace file or payload is read, no receipt input or merged receipt input is materialized, no receipt payload is received, validated, applied, or converted into approval and no secret value, `.env` content, Desktop config content, Desktop provider key, credential lookup, automatic redaction, deletion, rotation, remediation action, trading write, artifact mutation, release publication, git operation, or protected action occurs.
- P462: `trading:secret-scan-remediation-receipt-chain-secret-scan-remediation-receipt-approval-plan-fixtures` declares future approval plan rows for each P461 Secrets Scan Gate remediation receipt chain advisory-remediation receipt validation-packet row while no actor workspace file or payload is read, no receipt input or merged receipt input is materialized, no receipt payload is received, validated, applied, or converted into approval and no secret value, `.env` content, Desktop config content, Desktop provider key, credential lookup, automatic redaction, deletion, rotation, remediation action, trading write, artifact mutation, release publication, git operation, or protected action occurs.
- P463: `trading:secret-scan-remediation-receipt-chain-secret-scan-remediation-receipt-approval-closeout-fixtures` declares future approval closeout rows for each P462 Secrets Scan Gate remediation receipt chain advisory-remediation receipt approval-plan row while no actor workspace file or payload is read, no receipt input or merged receipt input is materialized, no receipt payload is received, validated, applied, or converted into approval and no secret value, `.env` content, Desktop config content, Desktop provider key, credential lookup, automatic redaction, deletion, rotation, remediation action, trading write, artifact mutation, release publication, git operation, or protected action occurs.
- P464: `trading:secret-scan-remediation-receipt-chain-secret-scan-remediation-receipt-closeout-fixtures` declares future receipt-chain closeout rows for each P463 Secrets Scan Gate remediation receipt chain advisory-remediation receipt approval-closeout row while human receipts remain pending, no actor workspace file or payload is read, no receipt input or merged receipt input is materialized, no receipt payload is received, validated, applied, or converted into approval and no secret value, `.env` content, Desktop config content, Desktop provider key, credential lookup, automatic redaction, deletion, rotation, remediation action, trading write, artifact mutation, release publication, git operation, or protected action occurs.
- P465: `trading:secret-scan-remediation-receipt-chain-secret-scan-remediation-receipt-chain-regression-fixtures` verifies P455-P464 Secrets Scan Gate remediation receipt chain advisory-remediation receipt commands, validation-chain entries, and ledger rows while human receipts remain pending, no receipt payload is received, validated, applied, or converted into approval and no secret value, `.env` content, Desktop config content, Desktop provider key, credential lookup, automatic redaction, deletion, rotation, remediation action, trading write, artifact mutation, release publication, git operation, or protected action occurs.
- Simulated broker adapter and live broker adapter contracts are separate.
- Live adapter files, if present in the future, are not imported by default control-plane paths.
- Live credentials use external secret handles only; plaintext, environment dumps, and provider keys are forbidden in repo artifacts.
- Secret boundary checks include Trading and Desktop companion configuration fixtures.
- Secret leakage regressions use synthetic pattern-only rows and redacted samples.
- Secrets Scan Gate wiring remains attached to the platform command, control-plane loop, contract validation, and Review API surfaces.
- Secrets Scan Gate attention states remain visible through dashboard counters and Review API filters without exposing secret material.
- Secrets Scan Gate fail-closed behavior remains asserted across every leakage category without materializing secret values.
- Secrets Scan Gate remediation remains advisory; fix/rerun actions do not become automatic redaction, deletion, rotation, apply, or protected-action commands.
- Secrets Scan Gate remediation receipt templates remain pending and reference-only until a human provides an external decision receipt.
- Secrets Scan Gate remediation receipt intake rows stay pending human input and do not materialize receipt payloads.
- Secrets Scan Gate remediation receipt validation rules are declared for future human payload checks only and do not validate or apply payloads.
- Secrets Scan Gate remediation receipt workspace rows are external-reference-only and do not read or materialize receipt files.

## P441-P460 Acceptance Criteria

- Recovery drills include stale data, exchange outage, failed fill, partial fill, duplicate order intent, kill switch, rollback-to-paper, and rollback-to-paper-after-live-halt fixtures.
- Recovery commands remain draft receipts and do not execute broker calls, shell commands, file restores, or protected actions.
- Recovery artifacts are visible to operator surfaces and remain human-review gated.

## P461-P480 Acceptance Criteria

- Dashboard/API surfaces explain why live/future mutation is blocked.
- Operator rows expose risk block/halt reason, disabled route, missing approval, rollback target, current stage, and next allowed action.
- Hermes Desktop remains a companion/operator surface and never becomes runtime source of truth.

## P481-P500 Acceptance Criteria

- Platform operations freeze proves every P341-P480 source is complete or explicitly blocked by a documented human gate.
- `platform:release-check -- --check`, `trading:release-check -- --check`, `npm run validate`, `npm test`, `contracts:validate -- --check`, `release:freeze -- --check`, and `control-plane:loop` pass.
- Trading live/full-auto/order submission, Desktop mutation/source-of-truth, protected recovery execution, and secret exposure remain false.

## Non-Goal

This program does not turn Hermes into a broker, deployment system, Desktop source
of truth, or autonomous mutation engine. It strengthens the platform's ability to
prove what is safe, blocked, reproducible, observable, and recoverable.
